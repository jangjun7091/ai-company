# Incidents

Store incident records here using `templates/incident.md` — one file per incident, written while running `company/workflows/incident-to-learning.md`. The record preserves the full timeline, root cause, and corrective action; the distilled lesson becomes an entry in `docs/learning/`.
